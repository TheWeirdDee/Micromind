'use client';
import { FormEvent, useEffect, useRef, useState } from 'react';
import { Headphones, MessageCircle, Send, X, AlertTriangle, CheckCircle2 } from 'lucide-react';
import { useAuth } from '@/context/AuthContext';
type Message={role:'user'|'assistant';content:string}; const API=process.env.NEXT_PUBLIC_AGENT_API_URL;
function getVisitorId(){const k='mm_support_visitor_id';let id=localStorage.getItem(k);if(!id){id=crypto.randomUUID();localStorage.setItem(k,id)}return id}
export function SupportWidget(){
 const{user,session}=useAuth();const[open,setOpen]=useState(false),[started,setStarted]=useState(false),[busy,setBusy]=useState(false);const[name,setName]=useState(''),[email,setEmail]=useState(''),[conversationId,setConversationId]=useState<string|null>(null),[draft,setDraft]=useState(''),[error,setError]=useState(''),[ticketId,setTicketId]=useState<string|null>(null);const[messages,setMessages]=useState<Message[]>([{role:'assistant',content:'Hi! I’m the MicroMind support assistant. What can I help you with?'}]);const endRef=useRef<HTMLDivElement>(null);
 useEffect(()=>{endRef.current?.scrollIntoView({behavior:'smooth'})},[messages,busy]);
 async function send(e:FormEvent){e.preventDefault();const text=draft.trim();if(!text||busy)return;setError('');setDraft('');setBusy(true);setMessages(m=>[...m,{role:'user',content:text}]);try{const r=await fetch(`${API}/api/support/chat`,{method:'POST',headers:{'Content-Type':'application/json',...(session?.access_token?{Authorization:`Bearer ${session.access_token}`}:{})},body:JSON.stringify({conversationId,visitorId:getVisitorId(),name:name.trim(),email:email.trim(),message:text,pageUrl:location.href})});const b=await r.json();if(!r.ok)throw new Error(b.error||'Support is unavailable right now.');setConversationId(b.conversationId);setMessages(m=>[...m,{role:'assistant',content:b.answer}]);if(b.ticketId)setTicketId(b.ticketId)}catch(err){setError((err as Error).message)}finally{setBusy(false)}}
 return <><button onClick={()=>{setOpen(v=>!v);if(!email&&user?.email)setEmail(user.email)}} aria-label="Open support chat" className="fixed z-50 bottom-5 right-5 w-14 h-14 rounded-full bg-accent text-bg shadow-2xl flex items-center justify-center hover:scale-105 transition-transform">{open?<X className="w-6 h-6"/>:<MessageCircle className="w-6 h-6"/>}</button>{open&&<section aria-label="MicroMind support" className="fixed z-50 bottom-24 right-4 sm:right-5 w-[calc(100vw-2rem)] sm:w-[380px] h-[min(620px,calc(100vh-8rem))] bg-surface border border-border rounded-3xl shadow-2xl overflow-hidden flex flex-col"><header className="p-4 border-b border-border bg-bg/50 flex items-center gap-3"><div className="w-9 h-9 rounded-xl bg-accent/10 text-accent flex items-center justify-center"><Headphones className="w-4 h-4"/></div><div><h2 className="font-serif text-base">MicroMind Support</h2><p className="font-mono text-[9px] uppercase tracking-wider text-text-muted">AI help · human escalation</p></div></header>{!started?<div className="p-5 space-y-4 overflow-y-auto"><div><h3 className="font-serif text-xl">How can we help?</h3><p className="font-mono text-xs text-text-muted mt-2 leading-relaxed">Enter your email so we can contact you if the AI needs to open a ticket.</p></div><label className="block text-[10px] font-mono uppercase text-text-muted">Name (optional)<input value={name} onChange={e=>setName(e.target.value)} maxLength={80} className="mt-1.5 w-full bg-bg border border-border rounded-xl px-3 py-2.5 text-sm normal-case"/></label><label className="block text-[10px] font-mono uppercase text-text-muted">Email<input type="email" required value={email} onChange={e=>setEmail(e.target.value)} maxLength={254} className="mt-1.5 w-full bg-bg border border-border rounded-xl px-3 py-2.5 text-sm normal-case"/></label><div className="flex gap-2 p-3 rounded-xl bg-amber-500/10 border border-amber-500/20 text-[10px] font-mono text-amber-200 leading-relaxed"><AlertTriangle className="w-4 h-4 shrink-0"/>Never share passwords, recovery phrases, private keys, payment details, or journal contents.</div><button disabled={!/^\S+@\S+\.\S+$/.test(email)} onClick={()=>setStarted(true)} className="w-full bg-accent text-bg rounded-xl py-3 font-serif font-bold disabled:opacity-40">Start chat</button><p className="text-[9px] font-mono text-text-muted text-center">Chats are saved and may be reviewed by MicroMind support.</p></div>:<><div className="flex-1 overflow-y-auto p-4 space-y-3">{messages.map((m,i)=><div key={i} className={`max-w-[88%] rounded-2xl px-3.5 py-2.5 text-xs leading-relaxed ${m.role==='user'?'ml-auto bg-accent text-bg':'bg-bg border border-border text-text-primary'}`}>{m.content}</div>)}{busy&&<div className="text-[10px] font-mono text-text-muted">Thinking…</div>}{ticketId&&<div className="p-3 rounded-xl bg-accent/10 border border-accent/20 text-xs"><CheckCircle2 className="inline w-4 h-4 text-accent mr-2"/>Ticket opened. We will reply to {email}. You can keep chatting here.<div className="font-mono text-[9px] text-text-muted mt-1">Reference: {ticketId.slice(0,8).toUpperCase()}</div></div>}{error&&<div className="text-xs text-red-400">{error}</div>}<div ref={endRef}/></div><form onSubmit={send} className="p-3 border-t border-border flex gap-2"><textarea value={draft} onChange={e=>setDraft(e.target.value)} maxLength={2000} rows={2} placeholder={ticketId?'Add more details or ask another question...':'Describe the issue...'} className="flex-1 resize-none bg-bg border border-border rounded-xl px-3 py-2 text-xs"/><button disabled={!draft.trim()||busy} className="w-10 rounded-xl bg-accent text-bg disabled:opacity-40 flex items-center justify-center"><Send className="w-4 h-4"/></button></form></>}</section>}</>}

